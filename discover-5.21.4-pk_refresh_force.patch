diff -up discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp.3~ discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp
--- discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp.3~	2026-03-03 10:29:38.000000000 +0100
+++ discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp	2026-03-03 17:21:45.521814904 +0100
@@ -605,7 +605,7 @@ void PackageKitBackend::checkForUpdates(
     if (!m_refresher) {
         acquireFetching(true);
         m_updater->clearDistroUpgrade();
-        auto refresh = PackageKit::Daemon::refreshCache(false);
+        auto refresh = PackageKit::Daemon::refreshCache(true);
         // Limit the cache-age so that we actually download new caches if necessary
         refresh->setHints(globalHints() << QStringLiteral("cache-age=300" /* 5 minutes */));
         setRefresher(refresh);
diff -up discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp.3~ discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp
--- discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp.3~	2026-03-03 10:29:38.000000000 +0100
+++ discover-6.6.2/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp	2026-03-03 16:34:09.312842692 +0100
@@ -62,7 +62,7 @@ PackageKitNotifier::PackageKitNotifier(Q
             bool ok;
             const int days = value.toInt(&ok);
             if (!ok || days == 0) {
-                regularCheck->setInterval(24h); // refresh at least once every day
+                regularCheck->setInterval(12h); // refresh at least once every day
                 regularCheck->start();
                 if (!value.isEmpty()) {
                     qWarning() << "couldn't understand value for timer:" << value;
@@ -301,7 +301,7 @@ void PackageKitNotifier::refreshDatabase
     }
 
     if (!m_refresher) {
-        m_refresher = PackageKit::Daemon::refreshCache(false);
+        m_refresher = PackageKit::Daemon::refreshCache(true);
         // Limit the cache-age so that we actually download new caches if necessary
         m_refresher->setHints(QStringLiteral("cache-age=300" /* 5 minutes */));
         connect(m_refresher.data(), &PackageKit::Transaction::finished, this, &PackageKitNotifier::recheckSystemUpdateNeeded);
